meta {
  name: List Characters
  type: http
  seq: 1
}

get {
  url: {{base_url}}/characters
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

script:post-response {
  if (res.status === 200 && Array.isArray(res.body) && res.body.length > 0) {
    bru.setEnvVar("character_id", res.body[0].id);
  }
}
