# Base URL of the Veloz API. Leave blank to call the same origin that serves
# the bundle (recommended for prod if you host the SPA behind the API/edge).
# In dev, Vite proxies /auth and /admin to localhost:81, so leave this blank.
VITE_API_BASE=
